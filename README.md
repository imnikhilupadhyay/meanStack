# meanStack Documentation
```
-Add IDE in enviroment varible inside advance setting for Desktop.

MongoDB Setup
-chmd 777 /data/db
-alias mongod="/c/Program\ Files/MongoDB/Server/3.6/bin/mongod.exe".
-Type mongod to start.

Git and GutHub Setup
-git init
-git remote add https://github.com/imnikhilupadhyay/meanStack.git
-git remote / git remote -v <branch-url>
-git push -u origin master (to push to gitHub repository)
-npm init

Setup Dependencies
-npm i express --save
-npm i morgon --save
-npm i nodemon --save
-npm i bower --save

-bower init
-bower i toaster --save
-bower i jquery --save
-bower i angular-route angular --save


-Download Heroku
-make procfile as file as web routing

mLab Setup
-make a mLab -> AWS -> sandbox account
-create user
-copy the connection string and paste in vscode
-copy the string paste in :bash



Heroku Setup
-heroku login
-heroku create
-heroku config:set NODE-ENV=production --app <appname>
-git push heroku master
-heroku ps:scale web=1 --app <appname> // it will allocate some resources to heroku to run

```
